//===== NEED Phase 0.30 - runtime verifier ===================================
//= TEST ONLY. GENERATED by p30_runtime.py. Registers nothing, changes nothing.
//============================================================================

-	script	NEED Phase30 Verify	-1,{
	end;

OnInit:
	debugmes "[P30] ---- verifier start ----";

	// ---- DevilTower npctalk identity: the KOREAN exname must resolve ----
	// The 16 repaired sites share only 2 identities, so each probe also
	// reports how many sites it covers - a bare "2 of 2" would read as a much
	// smaller result than the 16 sites request §14 asks about.
	debugmes "[P30][NPCTALK_KR_SITES] covers 7 site(s), resolves=" + (getnpcid(0, "마법 학자 아르티#tnm") ? 1 : 0) + " : 마법 학자 아르티#tnm";
	debugmes "[P30][NPCTALK_KR_SITES] covers 9 site(s), resolves=" + (getnpcid(0, "역사학자 쉡#tnm01") ? 1 : 0) + " : 역사학자 쉡#tnm01";
	.@ok = 0; .@no = 0;
	if (getnpcid(0, "마법 학자 아르티#tnm")) .@ok++; else { .@no++; if (.@no <= 15) debugmes "[P30][NPCTALK_KR_IDENTITIES] unresolved: 마법 학자 아르티#tnm"; }
	if (getnpcid(0, "역사학자 쉡#tnm01")) .@ok++; else { .@no++; if (.@no <= 15) debugmes "[P30][NPCTALK_KR_IDENTITIES] unresolved: 역사학자 쉡#tnm01"; }
	debugmes "[P30][NPCTALK_KR_IDENTITIES] total=2 resolved=" + .@ok + " unresolved=" + .@no;
	debugmes "[P30][NPCTALK_KR] 2 identity(ies) covering 16 repaired site(s)";

	// ---- and the ENGLISH string it used to be given must NOT ----
	.@ok = 0; .@no = 0;
	if (getnpcid(0, "Historian Shep#tnm01")) { .@ok++; debugmes "[P30][NPCTALK_EN] unexpectedly resolves: Historian Shep#tnm01"; } else .@no++;
	if (getnpcid(0, "Magic Scholar Artie#tnm")) { .@ok++; debugmes "[P30][NPCTALK_EN] unexpectedly resolves: Magic Scholar Artie#tnm"; } else .@no++;
	debugmes "[P30][NPCTALK_EN] total=2 resolved=" + .@ok + " unresolved=" + .@no;

	// ---- every map-scoped NPC header of the three patched files ----
	.@ok = 0; .@no = 0;
	if (getnpcid(0, "한#a1")) .@ok++; else { .@no++; if (.@no <= 15) debugmes "[P30][NPC] unresolved: 한#a1"; }
	if (getnpcid(0, "도엑#a1")) .@ok++; else { .@no++; if (.@no <= 15) debugmes "[P30][NPC] unresolved: 도엑#a1"; }
	if (getnpcid(0, "과학자 도연#a1")) .@ok++; else { .@no++; if (.@no <= 15) debugmes "[P30][NPC] unresolved: 과학자 도연#a1"; }
	if (getnpcid(0, "공중요새 입구 워프#1")) .@ok++; else { .@no++; if (.@no <= 15) debugmes "[P30][NPC] unresolved: 공중요새 입구 워프#1"; }
	if (getnpcid(0, "공중요새 입구 워프#2")) .@ok++; else { .@no++; if (.@no <= 15) debugmes "[P30][NPC] unresolved: 공중요새 입구 워프#2"; }
	if (getnpcid(0, "과학자 도연#a2")) .@ok++; else { .@no++; if (.@no <= 15) debugmes "[P30][NPC] unresolved: 과학자 도연#a2"; }
	if (getnpcid(0, "기록관리자 아카샤")) .@ok++; else { .@no++; if (.@no <= 15) debugmes "[P30][NPC] unresolved: 기록관리자 아카샤"; }
	if (getnpcid(0, "역사학자 쉡#tnm01")) .@ok++; else { .@no++; if (.@no <= 15) debugmes "[P30][NPC] unresolved: 역사학자 쉡#tnm01"; }
	if (getnpcid(0, "마법 학자 아르티#tnm")) .@ok++; else { .@no++; if (.@no <= 15) debugmes "[P30][NPC] unresolved: 마법 학자 아르티#tnm"; }
	if (getnpcid(0, "차원 이동 장치#tnm0")) .@ok++; else { .@no++; if (.@no <= 15) debugmes "[P30][NPC] unresolved: 차원 이동 장치#tnm0"; }
	if (getnpcid(0, "#tnmtks01")) .@ok++; else { .@no++; if (.@no <= 15) debugmes "[P30][NPC] unresolved: #tnmtks01"; }
	debugmes "[P30][NPC] total=11 resolved=" + .@ok + " unresolved=" + .@no;

	// ---- instance-scoped headers, counted but not required to resolve ----
	.@ok = 0; .@no = 0;
	if (getnpcid(0, "스테판 잭 어니스트 울프#sthd_01")) .@ok++; else .@no++;
	if (getnpcid(0, "dummy_skyfort")) .@ok++; else .@no++;
	if (getnpcid(0, "스테판 잭 어니스트 울프#sthd_02")) .@ok++; else .@no++;
	if (getnpcid(0, "#sthd_event_1")) .@ok++; else .@no++;
	if (getnpcid(0, "#sthd_event_1_boss")) .@ok++; else .@no++;
	if (getnpcid(0, "#sthd_event_2")) .@ok++; else .@no++;
	if (getnpcid(0, "#sthd_event_3")) .@ok++; else .@no++;
	if (getnpcid(0, "#sthd_event_4")) .@ok++; else .@no++;
	if (getnpcid(0, "#Stefan_Action1")) .@ok++; else .@no++;
	if (getnpcid(0, "#sthd_key_control")) .@ok++; else .@no++;
	if (getnpcid(0, "#sthd_move_back_a_1")) .@ok++; else .@no++;
	if (getnpcid(0, "#sthd_move_back_a_2")) .@ok++; else .@no++;
	if (getnpcid(0, "#sthd_move_back_a_3")) .@ok++; else .@no++;
	if (getnpcid(0, "#sthd_move_back_a_4")) .@ok++; else .@no++;
	if (getnpcid(0, "#sthd_move_back_a_5")) .@ok++; else .@no++;
	if (getnpcid(0, "#sthd_move_back_a_6")) .@ok++; else .@no++;
	if (getnpcid(0, "활성화된 워프#sthd_mov_1")) .@ok++; else .@no++;
	if (getnpcid(0, "활성화된 워프#sthd_mov_2")) .@ok++; else .@no++;
	if (getnpcid(0, "활성화된 워프#sthd_mov_3")) .@ok++; else .@no++;
	if (getnpcid(0, "활성화된 워프#sthd_mov_4")) .@ok++; else .@no++;
	if (getnpcid(0, "활성화된 워프#sthd_mov_5")) .@ok++; else .@no++;
	if (getnpcid(0, "활성화된 워프#sthd_mov_6")) .@ok++; else .@no++;
	if (getnpcid(0, "비활성화된 워프#sthd_mov_1")) .@ok++; else .@no++;
	if (getnpcid(0, "비활성화된 워프#sthd_mov_2")) .@ok++; else .@no++;
	if (getnpcid(0, "비활성화된 워프#sthd_mov_3")) .@ok++; else .@no++;
	if (getnpcid(0, "비활성화된 워프#sthd_mov_4")) .@ok++; else .@no++;
	if (getnpcid(0, "비활성화된 워프#sthd_mov_5")) .@ok++; else .@no++;
	if (getnpcid(0, "비활성화된 워프#sthd_mov_6")) .@ok++; else .@no++;
	if (getnpcid(0, "#sthd_event_13")) .@ok++; else .@no++;
	if (getnpcid(0, "#sthd_event_14")) .@ok++; else .@no++;
	if (getnpcid(0, "#sthd_event_15")) .@ok++; else .@no++;
	if (getnpcid(0, "#sthd_event_16")) .@ok++; else .@no++;
	if (getnpcid(0, "#sthd_event_17")) .@ok++; else .@no++;
	if (getnpcid(0, "#sthd_event_18")) .@ok++; else .@no++;
	if (getnpcid(0, "공중요새 황금 보물상자#1")) .@ok++; else .@no++;
	if (getnpcid(0, "공중요새 황금 보물상자#2")) .@ok++; else .@no++;
	if (getnpcid(0, "공중요새 황금 보물상자#3")) .@ok++; else .@no++;
	if (getnpcid(0, "공중요새 황금 보물상자#4")) .@ok++; else .@no++;
	if (getnpcid(0, "공중요새 보물상자")) .@ok++; else .@no++;
	if (getnpcid(0, "불멸의 저주 받은 기사#")) .@ok++; else .@no++;
	if (getnpcid(0, "실험 중인 윈드 고스트")) .@ok++; else .@no++;
	if (getnpcid(0, "불멸의 윈드 고스트#01")) .@ok++; else .@no++;
	if (getnpcid(0, "#sthd_event_5")) .@ok++; else .@no++;
	if (getnpcid(0, "#sthd_event_6")) .@ok++; else .@no++;
	if (getnpcid(0, "#sthd_event_7")) .@ok++; else .@no++;
	if (getnpcid(0, "#sthd_event_8")) .@ok++; else .@no++;
	if (getnpcid(0, "#sthd_event_9")) .@ok++; else .@no++;
	if (getnpcid(0, "#sthd_event_10")) .@ok++; else .@no++;
	if (getnpcid(0, "#sthd_event_11")) .@ok++; else .@no++;
	if (getnpcid(0, "#sthd_event_12")) .@ok++; else .@no++;
	if (getnpcid(0, "#Stefan_Action2")) .@ok++; else .@no++;
	if (getnpcid(0, "스테판 잭 어니스트 울프#sthd_03")) .@ok++; else .@no++;
	if (getnpcid(0, "스테판 잭 어니스트 울프#sthd_04")) .@ok++; else .@no++;
	if (getnpcid(0, "#Stefan for display3")) .@ok++; else .@no++;
	if (getnpcid(0, "#Last_Boss")) .@ok++; else .@no++;
	if (getnpcid(0, "#Last_Boss2_Monster")) .@ok++; else .@no++;
	if (getnpcid(0, "#Last_Boss_Monster")) .@ok++; else .@no++;
	if (getnpcid(0, "#pantheon_damage1")) .@ok++; else .@no++;
	if (getnpcid(0, "#pantheon_damage2")) .@ok++; else .@no++;
	if (getnpcid(0, "#pantheon_damage3")) .@ok++; else .@no++;
	if (getnpcid(0, "#pantheon_damage4")) .@ok++; else .@no++;
	if (getnpcid(0, "#pantheon_damage5")) .@ok++; else .@no++;
	if (getnpcid(0, "#pantheon_damage6")) .@ok++; else .@no++;
	if (getnpcid(0, "#pantheon_damage7")) .@ok++; else .@no++;
	if (getnpcid(0, "#pantheon_damage8")) .@ok++; else .@no++;
	if (getnpcid(0, "#pantheon_damage9")) .@ok++; else .@no++;
	if (getnpcid(0, "#pantheon_damage10")) .@ok++; else .@no++;
	if (getnpcid(0, "#pantheon_damage11")) .@ok++; else .@no++;
	if (getnpcid(0, "#pantheon_damage12")) .@ok++; else .@no++;
	if (getnpcid(0, "#pantheon_damage13")) .@ok++; else .@no++;
	if (getnpcid(0, "#pantheon_damage14")) .@ok++; else .@no++;
	if (getnpcid(0, "#pantheon_damage15")) .@ok++; else .@no++;
	if (getnpcid(0, "#pantheon_damage16")) .@ok++; else .@no++;
	if (getnpcid(0, "#pantheon_damage17")) .@ok++; else .@no++;
	if (getnpcid(0, "#pantheon_damage18")) .@ok++; else .@no++;
	if (getnpcid(0, "#pantheon_damage19")) .@ok++; else .@no++;
	if (getnpcid(0, "#pantheon_damage20")) .@ok++; else .@no++;
	if (getnpcid(0, "#pantheon_damage21")) .@ok++; else .@no++;
	if (getnpcid(0, "#pantheon_damage22")) .@ok++; else .@no++;
	if (getnpcid(0, "#pantheon_damage23")) .@ok++; else .@no++;
	if (getnpcid(0, "#pantheon_damage24")) .@ok++; else .@no++;
	if (getnpcid(0, "#pantheon_damage25")) .@ok++; else .@no++;
	if (getnpcid(0, "#pantheon_damage26")) .@ok++; else .@no++;
	if (getnpcid(0, "#pantheon_damage27")) .@ok++; else .@no++;
	if (getnpcid(0, "공중요새 탈출 워프#end_warp")) .@ok++; else .@no++;
	if (getnpcid(0, "#event01")) .@ok++; else .@no++;
	if (getnpcid(0, "장교 하임#heim0")) .@ok++; else .@no++;
	if (getnpcid(0, "장교 하임#heim")) .@ok++; else .@no++;
	if (getnpcid(0, "치료사 파마#feima")) .@ok++; else .@no++;
	if (getnpcid(0, "부상당한 병사#1_1")) .@ok++; else .@no++;
	if (getnpcid(0, "부상당한 병사#2_1")) .@ok++; else .@no++;
	if (getnpcid(0, "부상당한 병사#3_1")) .@ok++; else .@no++;
	if (getnpcid(0, "부상당한 병사#4_1")) .@ok++; else .@no++;
	if (getnpcid(0, "부상당한 병사#5_1")) .@ok++; else .@no++;
	if (getnpcid(0, "부상당한 병사#6_1")) .@ok++; else .@no++;
	if (getnpcid(0, "부상당한 병사#7_1")) .@ok++; else .@no++;
	if (getnpcid(0, "부상당한 병사#1")) .@ok++; else .@no++;
	if (getnpcid(0, "부상당한 병사#2")) .@ok++; else .@no++;
	if (getnpcid(0, "부상당한 병사#3")) .@ok++; else .@no++;
	if (getnpcid(0, "부상당한 병사#4")) .@ok++; else .@no++;
	if (getnpcid(0, "부상당한 병사#5")) .@ok++; else .@no++;
	if (getnpcid(0, "부상당한 병사#6")) .@ok++; else .@no++;
	if (getnpcid(0, "부상당한 병사#7")) .@ok++; else .@no++;
	if (getnpcid(0, "부관#tnm02")) .@ok++; else .@no++;
	if (getnpcid(0, "보병#tnm01")) .@ok++; else .@no++;
	if (getnpcid(0, "루실#tnm01")) .@ok++; else .@no++;
	if (getnpcid(0, "마법 검사#tnm01")) .@ok++; else .@no++;
	if (getnpcid(0, "어쌔신 휴이#tnm01")) .@ok++; else .@no++;
	if (getnpcid(0, "어쌔신 루이#tnm01")) .@ok++; else .@no++;
	if (getnpcid(0, "어쌔신 듀이#tnm01")) .@ok++; else .@no++;
	if (getnpcid(0, "#tnm1stepmob")) .@ok++; else .@no++;
	if (getnpcid(0, "어쌔신 루이#tnm02")) .@ok++; else .@no++;
	if (getnpcid(0, "루실#tnm02")) .@ok++; else .@no++;
	if (getnpcid(0, "휴이#hui02")) .@ok++; else .@no++;
	if (getnpcid(0, "마법 검사#tnm02")) .@ok++; else .@no++;
	if (getnpcid(0, "마왕 모로크#tnm01")) .@ok++; else .@no++;
	if (getnpcid(0, "루실#tnm03")) .@ok++; else .@no++;
	if (getnpcid(0, "#devil_flamecross")) .@ok++; else .@no++;
	if (getnpcid(0, "#devil_seal_timer")) .@ok++; else .@no++;
	if (getnpcid(0, "#devil_seal_spawn_global")) .@ok++; else .@no++;
	if (getnpcid(0, "마왕의 인장 소환#DIR_NORTHWEST")) .@ok++; else .@no++;
	if (getnpcid(0, "마왕의 인장 소환#DIR_SOUTHWEST")) .@ok++; else .@no++;
	if (getnpcid(0, "마왕의 인장 소환#DIR_SOUTHEAST")) .@ok++; else .@no++;
	if (getnpcid(0, "마왕의 인장 소환#DIR_NORTHEAST")) .@ok++; else .@no++;
	if (getnpcid(0, "마왕의 인장 소환#DIR_NORTH")) .@ok++; else .@no++;
	if (getnpcid(0, "인장 중앙 도달#DIR_NORTHWEST")) .@ok++; else .@no++;
	if (getnpcid(0, "인장 중앙 도달#DIR_SOUTHWEST")) .@ok++; else .@no++;
	if (getnpcid(0, "인장 중앙 도달#DIR_SOUTHEAST")) .@ok++; else .@no++;
	if (getnpcid(0, "인장 중앙 도달#DIR_NORTHEAST")) .@ok++; else .@no++;
	if (getnpcid(0, "인장 중앙 도달#DIR_NORTH")) .@ok++; else .@no++;
	if (getnpcid(0, "#devil_seal_dead")) .@ok++; else .@no++;
	if (getnpcid(0, "루실#tnm04")) .@ok++; else .@no++;
	if (getnpcid(0, "#tnm3event01")) .@ok++; else .@no++;
	if (getnpcid(0, "어린 소녀#tnm01")) .@ok++; else .@no++;
	if (getnpcid(0, "#tnm3event02ect")) .@ok++; else .@no++;
	if (getnpcid(0, "어린 소녀#tnm02")) .@ok++; else .@no++;
	if (getnpcid(0, "어린 소녀#tnm03")) .@ok++; else .@no++;
	if (getnpcid(0, "사악한 그림자#tnm3mob01")) .@ok++; else .@no++;
	if (getnpcid(0, "악의 그림자 처치#tnm3mob01")) .@ok++; else .@no++;
	if (getnpcid(0, "마물 그림자#mobmaster")) .@ok++; else .@no++;
	if (getnpcid(0, "#mobmaster_kill")) .@ok++; else .@no++;
	if (getnpcid(0, "모로크 로드나이트#mocl")) .@ok++; else .@no++;
	if (getnpcid(0, "로키#tnmloki01")) .@ok++; else .@no++;
	if (getnpcid(0, "어린 소녀#tnm04")) .@ok++; else .@no++;
	if (getnpcid(0, "#morocc_lord_talk")) .@ok++; else .@no++;
	if (getnpcid(0, "로키#tnmloki02")) .@ok++; else .@no++;
	if (getnpcid(0, "#boss_skills_timer")) .@ok++; else .@no++;
	if (getnpcid(0, "상자#tnmbosang")) .@ok++; else .@no++;
	if (getnpcid(0, "로키#tnmloki03")) .@ok++; else .@no++;
	debugmes "[P30][NPC_INSTANCE_SCOPED] total=149 resolved=" + .@ok + " not-live=" + .@no;

	debugmes "[P30][FUNC] function blocks in the three files=1 (parse-time only)";

	debugmes "[P30] ---- verifier done ----";
	bindatcmd "p30", "NEED Phase30 Verify::OnAtP30", 99, 99;
	end;

OnAtP30:
	// run once on a KR character and once on an EN character and compare
	dispbottom "[P30] need_lang=" + getneedlang();
	dispbottom "[00] " + needtr("[한]", "[Han]");
	dispbottom "[01] " + needtr("결국 내 가족과", "is what ultimately protects my family");
	dispbottom "[02] " + needtr("도연을 도와줘...", "Help Doyeon...");
	dispbottom "[03] " + needtr("베이스 레벨 145 이상이어야 합니다. -", "to proceed. -");
	dispbottom "[04] " + needtr("무사히 돌아오셨군요.", "You made it back safely.");
	dispbottom "[05] " + needtr("그래도 공중요새의 거대한 골렘을 쓰러뜨렸으니", "Still, you brought down the giant golem of the Sky Fortress,");
	dispbottom "[06] " + needtr("충분하지 않았던 것 같습니다.", "was not enough after all.");
	dispbottom "[07] " + needtr("공중요새에 입장한다.", "Enter the Sky Fortress.");
	dispbottom "[08] " + needtr("[기록관리자 아카샤]", "[Archivist Akasha]");
	dispbottom "[09] " + needtr("언제든 NEED Wiki를 열 수 있습니다.", "to open the NEED Wiki at any time.");
	dispbottom "[10] " + needtr("^FF9900상위 던전 및 콘텐츠^000000", "^FF9900Higher dungeons and content^000000");
	dispbottom "[11] " + needtr("콘텐츠는 시나리오 진행에 따라", "As the scenario advances, in what way");
	dispbottom "[12] " + needtr("악마의탑 탐사에 자원하러 오신 건가요?", "Are you volunteering to explore the Devil's Tower?");
	dispbottom "[13] " + needtr("그럼, 정식으로 자원하시겠습니까?", "So do you want to volunteer?");
	dispbottom "[14] " + needtr("[부상당한 병사들]", "[Injured soldiers]");
	dispbottom "[15] " + needtr("내가 길을 안내해 줄게.", "I will guide you.");
	dispbottom "[16] " + needtr("그곳에서 시공간이 뒤틀려 수렴하고 있다는 거군요?", "So space and time are twisting and converging there?");
	dispbottom "[17] " + needtr("오늘 해당 던전의 보상 이용 횟수를", "You have used up all the reward");
	dispbottom "[18] " + needtr("차원 장치가 활성화됐어요.", "The Dimensional Device is active.");
	dispbottom "[19] " + needtr("시스템 복구를 시작합니다. 잠시만 기다려 주세요.", "Starting the system recovery. Please wait a moment.");
	end;
}
